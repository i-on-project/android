package org.ionproject.android.common.dto.enums

/**
 * Represents an HTTP Method
 */
enum class HttpMethod {
    GET,
    POST,
    PUT,
    DELETE,
    HEAD,
    PATCH,
    CONNECT,
    OPTIONS,
    TRACE
}