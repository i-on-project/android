<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".courses.CoursesFragment"
    android:padding="@dimen/fragment_padding"
    >

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/courses_recyclerview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:listitem="@layout/courses_list_item"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/optional_courses_button"
        />

    <Button
        android:id="@+id/optional_courses_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/optional_courses"
        app:layout_constraintTop_toBottomOf="@id/courses_recyclerview">
    </Button>



</androidx.constraintlayout.widget.ConstraintLayout>